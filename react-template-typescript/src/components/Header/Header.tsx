import React, { FC } from "react";
import styles from "./Header.module.scss";

const Header: FC<any> = () => {
  return (
    <>
      <header>
        
      </header>
    </>
  );
};

export default React.memo(Header);
